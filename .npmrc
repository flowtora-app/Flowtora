# React 19 ships ahead of some libraries' declared peerDependency
# ranges. react-simple-maps@3.0.0 lists React 16-18 in peerDeps but
# works correctly on React 19; tell npm to install through the
# mismatch instead of erroring.
legacy-peer-deps=true
